{
  "$schema": "http://json-schema.org/draft-04/schema",
  "title": "JSON Schema for buildings",

  "type": "object",

  "definitions": {
    "resourceQuantity": {
      "type": "object",
      "properties": {
        "resource": {
          "type": "string"
        },
        "quantity": {
          "type": "integer",
          "minimum": 1
        }
      }
    }
  },

  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the building."
    },
    "type": {
      "type": "string",
      "enum": [ "production", "storage" ]
    },
    "width": {
      "type": "integer",
      "minimum": 1
    },
    "height": {
      "type": "integer",
      "minimum": 1
    },
    "color": {
      "type": "array",
      "minItems": 3,
      "maxItems": 3,
      "description": "The representative color of this building",
      "items": {
        "type": "number"
      }
    },
    "productionLines": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "inputs": {
            "type": "array",
            "items": { "$ref": "#/definitions/resourceQuantity" }
          },
          "outputs": {
            "type": "array",
            "items": { "$ref": "#/definitions/resourceQuantity" }
          }
        },
        "required": [ "inputs", "outputs" ]
      }
    },
    "buildCost": {
      "type": "object",
      "properties": {
        "workTicks": {
          "type": "number",
          "minimum": 0
        },
        "resources": {
          "type": "array",
          "items": { "$ref": "#/definitions/resourceQuantity" }
        }
      }
    }
  },
  "required": [ "name", "type", "width", "height", "color", "buildCost" ]
}
